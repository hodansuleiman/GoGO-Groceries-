<header>
    <nav class="navbar">
        <div class= "company-logo">insert logo</div> 
            <a href="#" class="toggle-button">
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
            </a>
        <div class="navbar-links">
            <ul>
                <% navbar.forEach(navbar => { %>
                    <li><a href="<%= navbar.toLowerCase() %>"><%= navbar %></a></li>
                <% }) %>
        </div>
    </nav>
</header>
